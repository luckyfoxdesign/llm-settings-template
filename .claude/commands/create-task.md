Create one implementation-ready task in `docs/backlog/todo/` without starting it.

Read and follow the full algorithm in workspace `AGENTS.md`, section `/create-task` Equivalent (`AGENTS.md#create-task-equivalent`).

Use the arguments and current conversation as task input. Ask only for missing information that would materially change the contract.

Do not move the task to WIP, implement it, or commit it.

ARGUMENTS: {{args}}
